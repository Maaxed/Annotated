ext {
	projectName = "$rootProject.baseProjectName-Lib"
	version = rootProject.libVersion
}
apply from: rootProject.file('/forge_dependency.gradle')

group = "${rootProject.group}.lib"

dependencies {
	testImplementation 'junit:junit:4.13'
}
