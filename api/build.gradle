plugins {
	id 'java'
}

ext {
	projectName = "$rootProject.baseProjectName-API"
	version = rootProject.apiVersion
}
apply from: rootProject.file('/configure_java.gradle')


group = "${rootProject.group}.api"
